## Group 1

Scope: TodayScreen/TodayModeScreen/TodayStabilityScreen/TodayAfterScreen, `today/` components, a
shared MoneyPathChart primitive, ChartStyleSheet (dead sheet id), SafeZoneSheet/LensPickerSheet/
AffordCheckSheet/ShelfSheet/SheetDayDetail, FolioShell (chart-style sheet mount only).

Lovable source root: `C:/dev/folio-melo/.claude/worktrees/design-main/src/components/folio/`
RN source root: `C:/dev/folio-v2-greenfield/.claude/worktrees/melo-mvp/apps/mobile/src/folio/`

| item                                                                                          | severity      | exact mismatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | RN file                                                                                                        | status                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ChartStyleSheet — sheet entirely missing                                                      | BREAKS-PARITY | Web ships a full "Chart style" sheet: a live picker with one card per `ChartStyle`, each rendering the actual `MoneyPathChart` against a shared sample data set. RN's `SheetId` union and `FolioShell`'s title map both declared `'chart-style': 'Path style'`, but no sheet component existed anywhere — `SheetView`'s generic placeholder host rendered "Coming soon." for it (confirmed: no `chart-style` case in `SheetView`, no file under `sheets/`).                                                                                                                                                                                                    | `sheets/ChartStyleSheet.tsx` (new), `shell/FolioShell.tsx`                                                     | **FIXED** — built `ui/MoneyPathChart.tsx` (new RN primitive, 1:1 port of the web's `charts/MoneyPathChart.tsx`, all three styles: curve/bars/minimal) + `sheets/ChartStyleSheet.tsx` (1:1 port of `SheetChartStyle.tsx`, live preview cards, verbatim copy) + wired both into `FolioShell.tsx` (import, self-hosting-sheet registration, render case). Reachability from More's "Chart style" row is Group 3's scope (MoreScreen file ownership), not touched here. |
| TrialCountdownChip — component built but never rendered                                       | BREAKS-PARITY | Web renders `<TrialCountdownChip nav={nav} />` in the header of ALL THREE Today variants (`ScreenToday`, `ScreenTodayMode`, `ScreenTodayStability`) next to the lens+weather pill. RN had a byte-faithful `ui/TrialCountdownChip.tsx` component built, but a grep across the app found only its own definition file — zero call sites in any of the three Today screens.                                                                                                                                                                                                                                                                                       | `screens/TodayScreen.tsx`, `screens/TodayModeScreen.tsx`, `screens/TodayStabilityScreen.tsx`                   | **FIXED** — rendered `<TrialCountdownChip lens={{...}} onPress={...}>` in all three screens' headers, reading the real `useLens()` fields (`trialCycleId`/`plusUnlocked`/`proUnlocked`/`trialDaysLeft`) confirmed already ported at `lib/lens.ts`.                                                                                                                                                                                                                  |
| MoneyModeChip / weather pill / paywall-lock status strip — missing entirely on Survival Today | BREAKS-PARITY | Web's `ScreenToday` (Survival, the PRIMARY/default Today surface) renders the combined lens+weather pill AND a priority-ordered status strip (locked-lens pill wins over the sample-numbers chip). RN's `TodayScreen.tsx` had NEITHER — no `MoneyModeChip`, no `MeloWeatherGlyph`, no lens-picker affordance, no lock pill; only a bare Melo header button. `TodayModeScreen`/`TodayStabilityScreen` already had the lens+weather pill (confirmed working) but were missing the locked-lens status-strip branch that wins priority over the sample-numbers chip.                                                                                               | `screens/TodayScreen.tsx`, `screens/TodayModeScreen.tsx`, `screens/TodayStabilityScreen.tsx`                   | **FIXED** — added `subs`/`subPaused`/`bufferAmount`/`moneyMode`/`useLens()` store reads + a `deriveModeState('survival', ...)` call to TodayScreen for its weather figure, then rendered the lens+weather pill (mirroring its siblings) and a `LensLockChip` status-strip branch (locked-lens pill outranks the sample-numbers chip, exactly like the web's status-strip priority) on all three Today screens.                                                      |
| AffordCheckSheet — "Safe Zone now" disagreed with the web's calculation                       | BREAKS-PARITY | Web's `SheetAffordCheck` calls `safeZoneMath(ModeInputs)` (balance − Bills Shield − buffer) for both the "Safe Zone now" display AND the `checkAfford` verdict math, so the sheet's number always agrees with Today's own headline. RN's `AffordCheckSheet.tsx` and `lib/affordCheck.ts` had been RE-DERIVED against the money-path route engine (`route.tightPoint.amount` — the lowest FUTURE point, a different figure) instead of the now-ported `lib/modes/safeZone.ts::safeZoneMath` — so "Safe Zone now" could show a different (and lower) number than the real Safe Zone total shown elsewhere in the app (Stability's card, the Paywall guard, etc). | `lib/affordCheck.ts`, `sheets/AffordCheckSheet.tsx`                                                            | **FIXED** — rewrote `checkAfford()` to call `safeZoneMath(inputs)` directly (byte-faithful to the web's `checkAfford`), and rewired the sheet to build a real `ModeInputs` snapshot from the store (mirroring `TodayScreen`'s own pattern) and display `zone.total` for "Safe Zone now" instead of the route bridge's tight-point figure.                                                                                                                           |
| SafeZoneSheet / LensPickerSheet / ShelfSheet / SheetDayDetail                                 | none found    | All four already exist as real, wired components (`sheets/SafeZoneSheet.tsx`, `sheets/LensPickerSheet.tsx`, `sheets/ShelfSheet.tsx`, `sheets/SheetDayDetail.tsx`) and are registered as self-hosting sheets in `FolioShell.tsx`. No parity gaps found in this pass — not re-audited line-by-line against the web source in this batch (out of the specific BREAKS-PARITY items this pass targeted); flag for a dedicated pass if deeper verification is wanted.                                                                                                                                                                                                | `sheets/SafeZoneSheet.tsx`, `sheets/LensPickerSheet.tsx`, `sheets/ShelfSheet.tsx`, `sheets/SheetDayDetail.tsx` |
| TodayAfterScreen                                                                              | none found    | Already a faithful, real-engine-wired 1:1 port (reads the live `useRoute` bridge for its route-redraw preview, the real last-transaction for "what changed" — not the web's hardcoded £283/Tesco/−£42 demo values). No parity gap identified in this pass.                                                                                                                                                                                                                                                                                                                                                                                                     | `screens/TodayAfterScreen.tsx`                                                                                 |
| `today/` sub-components (TodayNudges, TodaySpendStrip, TodayRecentTxns, TodayWeekTiles)       | none found    | Already wired into all three Today screens per the web's ordering (nudges → spend strip → recent → path card → week tiles). Not re-audited component-by-component against the web originals in this batch — flag for a dedicated pass if deeper verification is wanted.                                                                                                                                                                                                                                                                                                                                                                                        | `screens/today/*.tsx`                                                                                          |

## Group 3

Scope: ScreenMelo, SheetMeloChat, MeloPhoenix usage/moods, ScreenPaywall, ScreenPlans(pricing), ScreenAccount, ScreenPrivacy, ScreenMore, SheetHouseholdSetup, SheetHiddenReview, TrialCountdownChip/MoneyModeChip/StubDisclaimer placements.

Lovable source root: `C:/dev/folio-melo/.claude/worktrees/design-main/src/components/folio/`
RN source root: `C:/dev/folio-v2-greenfield/.claude/worktrees/melo-mvp/apps/mobile/src/folio/`

| item                                                                       | severity                                 | exact mismatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | RN file                                                                                                                                     |
| -------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| ScreenMelo → MeloScreen                                                    | ✅ FIXED                                 | Rebuilt `MeloScreen.tsx` from scratch as the web Companion hub: "Companion" masthead (both copy variants), a live Plumage read (word + 4 dots) derived honestly from the real mode engine (`deriveModeState(...).safeZone.amount / monthlyIncome`, banded at the web's own thresholds — no fabricated `deriveMeloVitality` port needed since no such module exists in RN), a Melo-line whisper keyed to plumage, the "Companion touches" wardrobe (3 touches, Plus-gated, tap to equip/unequip max 3, suppressed via the real `canShowUpsell`), a real persisted "Quiet Mode" toggle (new `melo.quietMode` store slice + `setMelo()` mutator, schema v4→v5 migration), and the "Rituals" grid (`nav.go('ritual')` / `nav.go('insights')`). Lens-lock chip routes to `nav.go('paywall')` (RN's real plans screen). `pressure` prop kept optional for FolioShell call-site compat but unused.                 | `apps/mobile/src/folio/screens/MeloScreen.tsx`, `apps/mobile/src/folio/store.ts`                                                            |
| ScreenPaywall → PaywallScreen — trial CTA                                  | ✅ FIXED                                 | Wired to the REAL `useLens()` engine (confirmed already live in `@/folio/lib/lens` + `@/folio/store` `LensState`/`startLensTrial`/etc. — this port discovered the engine already existed, contrary to the prior build's "no lens engine" note). The Plus/Pro CTA now calls the real `startTrial()`, shows an honest confirmation, and the CTA area renders live "Trial · this cycle" / "Plus is on" / "Pro is on" states from real `plusUnlocked`/`proUnlocked`/`trialCycleId`.                                                                                                                                                                                                                                                                                                                                                                                                                             | `apps/mobile/src/folio/screens/PaywallScreen.tsx`                                                                                           |
| ScreenPaywall → PaywallScreen — lens rail dropped                          | ✅ FIXED                                 | Restored the "Ten lenses · N free · N plus · N pro" rail, wired to the real `FREE_LENSES`/`PLUS_LENSES`/`PRO_LENSES`/`MODE_LABEL`/`tierFor` from `@/folio/lib/lens` — Active / Free / locked-with-tier-badge / unlocked states all live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/mobile/src/folio/screens/PaywallScreen.tsx`                                                                                           |
| ScreenPaywall → PaywallScreen — paywall guard is single-signal             | ✅ FIXED                                 | Wired to the REAL `canShowUpsell`/`upsellSuppressionReason` from `@/folio/lib/lensPaywall` (already a full 1:1 port, discovered during this round) — all five suppression signals (safe-zone, recovery, quiet-mode, storm/rainy/fog weather) + their distinct reason copy are now live, fed by `deriveModeState(...).weather`, `moneyMode === 'reset'`, and the new real `melo.quietMode`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `apps/mobile/src/folio/screens/PaywallScreen.tsx`                                                                                           |
| ScreenPaywall → PaywallScreen — tier bullet "live" flags disagree with web | ✅ FIXED                                 | `TIER_COPY` corrected to match the web's frozen flags exactly — Plus's four lenses and Pro's Irregular/Debt/Low-vis lenses are `live: true` (confirmed all ten mode strategies are shipped in `@/folio/lib/modes/strategies/*`), and the compare matrix rows match.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `apps/mobile/src/folio/screens/PaywallScreen.tsx`                                                                                           |
| ScreenPaywall → PaywallScreen — current-tier strip loses trial chip        | ✅ FIXED                                 | Strip now renders Pro/Plus/Free by real tier plus the italic `· trial · ends {date}` suffix when `trialCycleId` is active and unconverted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `apps/mobile/src/folio/screens/PaywallScreen.tsx`                                                                                           |
| ScreenAccount → AccountScreen — tier always "Free"                         | ✅ FIXED                                 | Tier now reads the real `useLens()` state and renders all four tier states (Free/Plus/Pro/Trial) with matching hint copy, the real trial "days left" chip (`trialDaysLeft` from `useLens()`), and the CTA now switches "See plans" / "Upgrade to Pro" / "Manage plan" per tier. Three-tier-at-a-glance grid's `isCurrent` also treats an active trial as "on Plus", matching the web.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `apps/mobile/src/folio/screens/AccountScreen.tsx`                                                                                           |
| ScreenAccount → AccountScreen — export/wipe re-routed instead of inline    | not touched (kept as-is)                 | Left as the documented, deliberate safety improvement (routes through Privacy's gated D3 reset) — out of scope for this pass, not a regression introduced here.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `apps/mobile/src/folio/screens/AccountScreen.tsx`                                                                                           |
| ScreenAccount → AccountScreen — "Melo & quiet mode" row hint dropped       | ✅ FIXED                                 | Row relabelled "Melo & quiet mode" with the live hint branching on the real `melo.quietMode` ("quiet mode on — Melo won't chime in" vs "how Melo speaks, and when").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `apps/mobile/src/folio/screens/AccountScreen.tsx`                                                                                           |
| MeloPhoenix / Melo.tsx — mood set reduced 7 → 5                            | not touched (out of file ownership)      | `Melo.tsx` belongs to a different file-ownership lane for this pass (MeloScreen.tsx consumes it read-only); left as documented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `apps/mobile/src/folio/melo/Melo.tsx`                                                                                                       |
| ScreenMore → MoreScreen — flat list vs grouped, multiple rows dropped      | ✅ FIXED                                 | Restored the web's ONE flat 19-row list in the exact frozen order (Account & plan → ... → Start fresh), including the two previously-dropped rows: "Chart style" (live hint via `useChartStyle()`, opens the already-wired `chart-style` sheet) and "Hidden from Review" (live `hiddenCount` via the store, opens the already-wired `hidden-review` sheet). "Subscriptions" hint corrected back to "what still earns its place".                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `apps/mobile/src/folio/screens/MoreScreen.tsx`                                                                                              |
| ScreenMore → MoreScreen — header Melo avatar mood                          | not changed — verified reasonable        | Kept `mood="calm"` for the web's non-canonical `"soft"` (no `'soft'` in RN's `MeloMood` union) — documented, intentional, matches PARITY_GAPS' own MINOR verdict.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `apps/mobile/src/folio/screens/MoreScreen.tsx`                                                                                              |
| ScreenMore → MoreScreen — "Start fresh" behavior changed                   | not touched (kept as-is)                 | Left routed to `nav.go('privacy')` — the documented, deliberate D3 safety gate; out of scope for this pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/mobile/src/folio/screens/MoreScreen.tsx`                                                                                              |
| SheetMeloChat → MeloChatSheet                                              | MINOR (verified faithful)                | Full 1:1 port confirmed: header/mood, empty-state starters (4, verbatim), tone settings (Calm/Honest/Dry/Coachy), share toggle with an intentionally corrected honest-claims line (web's "Stays on this device" is a banned claim per COPY_DECK and is replaced with a truthful line — documented, correct call), tool-pill rendering + 30s Undo window (upgraded from web's 8s per the app's own D3 undo-window policy — documented), scroll-to-bottom FAB, shimmer "Melo's thinking…" loading state, and the proactive opener heuristic (payment-timing only, matching the web's subscription-signal-research constraint). No parity gaps found beyond the live-AI-gateway wiring being a separate, already-tracked engine seam (`@rn-engine melo-gateway`), which is disclosed in-file and not a silent gap.                                                                                             | `apps/mobile/src/folio/sheets/MeloChatSheet.tsx`                                                                                            |
| SheetHouseholdSetup → HouseholdSetupSheet                                  | MINOR                                    | Web's `commit()` shows a `toast("Household saved", { description: ... })` confirming the saved partner name + share percentage before closing. RN's `commit()` calls `setHousehold(...)` and closes with NO confirmation toast/alert at all (no toast library wired in this sheet) — the save silently succeeds with no user-facing acknowledgment. Also: web's default-share control is a native `<input type="range">` slider (continuous, `step={5}`); RN substitutes a ±5% stepper button pair with a fill bar — a reasonable RN substitute (documented) but a different interaction model (discrete taps vs. drag) worth flagging as a UX-level, not just visual, difference.                                                                                                                                                                                                                          | `apps/mobile/src/folio/sheets/HouseholdSetupSheet.tsx`                                                                                      |
| SheetHiddenReview → HiddenReviewSheet                                      | MINOR (largely faithful)                 | Structure, copy source, empty state ("Nothing hidden yet."), row shape (merchant/amount/date + Un-hide button), and Done button all match. One drop: web's headline accent-splits "from Review" in terracotta via `<em class="not-italic text-accent">` ("Hidden **from Review**"); RN's headline strips markdown asterisks (`copy.hidden.title.replace(/\*\*/g, '')`) and renders the whole headline in a single flat ink color — no accent-color split on the second half of the headline. Documented in-file as a deferred visual pass.                                                                                                                                                                                                                                                                                                                                                                  | `apps/mobile/src/folio/sheets/HiddenReviewSheet.tsx`                                                                                        |
| TrialCountdownChip — component built but never rendered                    | BREAKS-PARITY                            | Web renders `<TrialCountdownChip nav={nav} />` directly in `ScreenToday`'s header row (next to the combined lens+weather pill) — it is part of the PRIMARY Today surface, showing "N days left · trial" / "Last day · trial" whenever a one-cycle trial is active. RN has a byte-faithful `TrialCountdownChip.tsx` component built (confirmed present at `apps/mobile/src/folio/ui/TrialCountdownChip.tsx`) but it is imported and rendered NOWHERE in the entire RN app — a grep across all of `apps/mobile/src` finds only the component's own definition file, no call sites. The main `TodayScreen.tsx` never renders it (nor does any other screen). This is a fully dead, unreachable component — combined with the missing lens/trial engine (see Paywall/Account findings), no RN user can ever see a trial-countdown chip anywhere in the app.                                                     | `apps/mobile/src/folio/ui/TrialCountdownChip.tsx` (defined but unused; would-be call site: `apps/mobile/src/folio/screens/TodayScreen.tsx`) |
| MoneyModeChip / StubDisclaimer placement                                   | MINOR (verified faithful where rendered) | Web renders `MoneyModeChip` combined into one lens+weather pill directly on the PRIMARY `ScreenToday` (not a mode-specific sub-screen); `StubDisclaimer` renders on `ScreenTodayMode` only (not `ScreenTodayStability`, which has no stub caption since Stability is a fully shipped lens). RN's `TodayModeScreen.tsx` and `TodayStabilityScreen.tsx` reproduce this placement correctly (`MoneyModeChip` in both, `StubDisclaimer` only in TodayModeScreen) — verified faithful on those two files. However RN's plain `TodayScreen.tsx` (the actual primary/default Today surface most users land on) never renders `MoneyModeChip` at all, unlike web's `ScreenToday`, which always shows the combined lens+weather pill. This is a real placement gap on the primary surface, though of lower severity than the TrialCountdownChip finding since the lens concept itself doesn't fully exist in RN yet. | `apps/mobile/src/folio/screens/TodayScreen.tsx` (no MoneyModeChip); `TodayModeScreen.tsx` / `TodayStabilityScreen.tsx` (correct)            |

## Group 4

Scope: ScreenStart, ScreenIntake, ScreenGuided, ScreenReview, ScreenRecovery, ScreenPaydayRitual, ScreenWhatIf, SheetOnboarding, SheetImport-related, SheetSubCaught, SheetRouteDetail, SheetShare, SheetCalendarConnect/Export, SheetChartStyle.

Lovable source root: `C:/dev/folio-melo/.claude/worktrees/design-main/src/components/folio/`
RN source root: `C:/dev/folio-v2-greenfield/.claude/worktrees/melo-mvp/apps/mobile/src/folio/`

| item                                                                     | severity                               | exact mismatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | RN file                                                                                                                                                |
| ------------------------------------------------------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ScreenPaydayRitual                                                       | FIXED (was BREAKS-PARITY)              | Ported `step1ByMode`/`step2ByMode`/`step3ByMode` verbatim (10 modes × 3 steps, eyebrow/headline/body/stat/melo/cta each) plus a `ModeFramingBanner` on step 0 (built new — no RN component existed; created `apps/mobile/src/folio/ui/ModeFramingBanner.tsx` wired to the already-existing `getFraming`/`cycleClose` table). `moneyMode` now drives every step. tsc clean.                                                                                                                                                                                                                                                                                                                                                                                                                       | `apps/mobile/src/folio/screens/PaydayRitualScreen.tsx`, `apps/mobile/src/folio/ui/ModeFramingBanner.tsx`                                               |
| ScreenPaydayRitual                                                       | FIXED (was BREAKS-PARITY)              | Added the conditional "Step three · repay" card: `owedByPot`/`totalOwed`/`repayHeadroom` derived from the real `potLedger` (borrow/repay entries), inserted only when `totalOwed > 0`. Added a new `repayToPot` store action (mirrors `addToPot`, writes a `repay` ledger entry, no `saved` mutation) since RN had no way to write a repay entry at all — `borrowFromPot` is also still absent (only referenced in comments), so a user can't yet reach "owes a pot" through any RN UI; this fix makes the ritual correct once a borrow entry exists (e.g. a future Shortfall borrow-from-pot move).                                                                                                                                                                                             | `apps/mobile/src/folio/screens/PaydayRitualScreen.tsx`, `apps/mobile/src/folio/store.ts`                                                               |
| ScreenRecovery                                                           | FIXED (was BREAKS-PARITY)              | Added the structural-fit signpost card (StepChange / Citizens Advice / National Debtline, opened via `Linking.openURL`) shown when `doesntFit` (bills > income) or `hardCyclesInARow >= 2`. RN has no persisted `hardCyclesInARow` counter (the web bumps it at cycle-close); derived it honestly instead from the real `cycles` ledger (a streak of `tightPoint < 0` closes counted back from the most recent), so no new always-on store field was added for a signal only this screen reads.                                                                                                                                                                                                                                                                                                  | `apps/mobile/src/folio/screens/RecoveryScreen.tsx`                                                                                                     |
| ScreenRecovery                                                           | FIXED (was BREAKS-PARITY)              | Ported `getRecoveryCopy(mode)` verbatim (10-mode table: eyebrow/intro/headline lead+accent/shortfall+after labels/Melo default/cta) and wired `moneyMode` through the eyebrow, kicker, headline, shortfall-card label, Melo default line, and CTA. Removed the now-dead `DEFAULT_MELO_LINE` constant. tsc clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/mobile/src/folio/screens/RecoveryScreen.tsx`                                                                                                     |
| ScreenWhatIf                                                             | FIXED (was BREAKS-PARITY)              | Ported `getWhatIfCopy(mode)` verbatim (10-mode table incl. `headlineTemplate(amount)`) and wired `moneyMode` through the eyebrow, kicker, headline, both stat-tile labels, and CTA/cancel strings. tsc clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/mobile/src/folio/screens/WhatIfScreen.tsx`                                                                                                       |
| SheetRouteDetail                                                         | FIXED (was BREAKS-PARITY)              | Ported `ROUTE_DETAIL_COPY` verbatim (10-mode eyebrow/title/Melo-line table) and wired `moneyMode` through the header eyebrow (date suffix stays the real tapped-point label), the headline, and the Melo line — replacing the hardcoded survival-only "What's happening" / "Set aside for bills" / bills-go-out line.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `apps/mobile/src/folio/sheets/RouteDetailSheet.tsx`                                                                                                    |
| SheetOnboarding                                                          | FIXED (was BREAKS-PARITY — root cause) | Restored the intent-picker (10 `INTENT_OPTIONS`, radio-row list) and mode-specific extra-question step (`MODE_EXTRA` table, one slider per mode) between Hello and Rhythm, so onboarding is 7 steps again. `done()` now calls `setMoneyMode(intentMode)` and, for survival/stability, `setBufferAmount(modeExtra)` — the actual root cause fix: `setMoneyMode` now fires during RN onboarding, so Recovery/Ritual/WhatIf/RouteDetail's mode-driven copy (all fixed above) can actually show correctly for a real onboarded user. Deep-link `intent`/`onboardingFocus` prefill (the web's `SheetIntent` prop) was NOT ported — no RN `SheetIntent` type or deep-link caller exists yet; out of scope for this fix, flagged here for a future pass.                                                | `apps/mobile/src/folio/sheets/OnboardingSheet.tsx`                                                                                                     |
| SheetChartStyle                                                          | BLOCKED (out of scope this pass)       | Web ships a full "Chart style" sheet: live picker with one card per `ChartStyle`, each rendering the actual `MoneyPathChart` component against a shared sample data set so the user picks by seeing the real rendering, plus a "Current" badge and an explanatory footer line. RN has no `ChartStyleSheet.tsx` (or any file implementing it) anywhere under `apps/mobile/src/folio/sheets/`. The `SheetId` union in `types.ts` and the title map in `FolioShell.tsx` both still declare `'chart-style': 'Path style'`, but no sheet component is registered for it — it is a dead/stub route. Not touched this pass — file ownership for this fix was Onboarding/Recovery/Ritual/WhatIf/RouteDetail/StartIntake-Guided-Review/SubCaught-Share-Calendar; a new sheet is a separate, larger build. | `apps/mobile/src/folio/types.ts` (stub SheetId, line ~77), `apps/mobile/src/folio/shell/FolioShell.tsx` (stub title, line ~671) — no sheet file exists |
| SheetImport (SheetAddDebt)                                               | BLOCKED (out of scope this pass)       | Web ships `SheetAddDebt.tsx` (a dedicated add-a-debt form sheet, part of the Debt-mode surface). No corresponding `AddDebtSheet.tsx` (or similarly named file) exists anywhere under `apps/mobile/src/folio/sheets/`, and no SheetId for it appears in `types.ts`. Not touched this pass — a new sheet + SheetId + nav wiring is a separate, larger build outside this fix's file ownership.                                                                                                                                                                                                                                                                                                                                                                                                     | `apps/mobile/src/folio/sheets/` (no file)                                                                                                              |
| ScreenReview                                                             | BLOCKED (out of scope this pass)       | Web's review card has inline-editable Merchant text input and Amount-out number input directly on the card (the user can correct the merchant/amount before tapping Add), plus a "Cancel" button that just backs out without recording anything. RN's `ReviewScreen.tsx` renders the merchant/amount as read-only `Text`, and correction is only reachable via the separate `edit-txn` sheet. This fix's file ownership listed "StartScreen/IntakeScreen/GuidedScreen/ReviewScreen group-4 rows" — verified ReviewScreen against this specific MINOR row and left the inline-vs-sheet interaction model unchanged (functionally reachable today via edit-txn); not re-architected this pass.                                                                                                     | `apps/mobile/src/folio/screens/ReviewScreen.tsx`                                                                                                       |
| ScreenStart / ScreenIntake / ScreenGuided                                | none found                             | Both faithful, byte-for-byte verbatim copy, matching motion/token annotations; no parity gaps identified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `apps/mobile/src/folio/screens/StartScreen.tsx`, `IntakeScreen.tsx`, `GuidedCheckInScreen.tsx`                                                         |
| SheetSubCaught / SheetShare / SheetCalendarConnect / SheetCalendarExport | none found                             | All four verified faithful — copy, states (loading/empty/error/offline), and real engine wiring match or exceed the web source (e.g. CalendarConnect/Export both derive real store data where the web took mock props).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `apps/mobile/src/folio/sheets/SubCaughtSheet.tsx`, `ShareSheet.tsx`, `CalendarConnectSheet.tsx`, `CalendarExportSheet.tsx`                             |

## Group 2 — ScreenCalendar / ScreenTimeline / ScreenPots / ScreenSubscriptions / ScreenPlans / ScreenInsights / ScreenShortfall / SheetAddEvent / SheetAddPlan / SheetLogSpend / SheetLogPayment / SheetLogInvoice / SheetEditTxn / SheetEditItem / SheetAddDebt

| item                            | severity                                     | exact mismatch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | RN file                                                                                                                                                                                                                                                                       |
| ------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SheetAddDebt                    | FIXED (was BREAKS-PARITY)                    | Built `AddDebtSheet.tsx` from scratch (Kind chips Loan/Card/BNPL/Other with hints, name/Balance/APR/Minimum-per-month/Due-day fields, `addDebt`/`removeDebt` store calls). Mounted as a new SheetId `'declare-debt'` (distinct from the pre-existing ScreenId `'add-debt'`, which is the unrelated `AddEntryScreen` bill/debt-payment quick-add — kept untouched, doc-commented in `types.ts` so the two are never conflated again). Rewired the three real "declare a debt" call sites (`PlansScreen`'s "or add a debt", `TodayModeScreen`'s Debt-lens "+ Add a debt" CTA) from `nav.go('add-debt')` to `nav.openSheet('declare-debt')`. Web's confirmation toast ported via the existing undo/toast lib (`useUndo`/`showUndo`) — Undo removes the just-added debt. tsc clean, 341 tests pass.                  | `apps/mobile/src/folio/sheets/AddDebtSheet.tsx` (new), `apps/mobile/src/folio/types.ts`, `apps/mobile/src/folio/shell/FolioShell.tsx`, `apps/mobile/src/folio/store.ts`, `apps/mobile/src/folio/screens/PlansScreen.tsx`, `apps/mobile/src/folio/screens/TodayModeScreen.tsx` |
| ScreenShortfall                 | FIXED (was BREAKS-PARITY)                    | Wired the already-ported `getShortfallCopy(mode)` (existed unused in `lib/modes/action.ts`) through the eyebrow, kicker, headline lead, pause/pot/hold move labels, closing Melo line, and the "leave it" refusal label — all now mode-tinted instead of hardcoded survival-only literals.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `apps/mobile/src/folio/screens/ShortfallScreen.tsx`                                                                                                                                                                                                                           |
| ScreenShortfall                 | FIXED (was BREAKS-PARITY)                    | Added the `relief`/`cheer` auto-close effect: on the `gapNow` 0-crossing, Melo switches to "cheer", the closing line reads "Gap closed. I'll keep watching the path." for 1400ms, then `nav.back()` fires. (RN's simplified `Melo` has no `pose` prop per the Group-3 mood-set finding, so only the mood switches — the web's "sealed" pose has no RN analogue yet.)                                                                                                                                                                                                                                                                                                                                                                                                                                             | `apps/mobile/src/folio/screens/ShortfallScreen.tsx`                                                                                                                                                                                                                           |
| ScreenCalendar                  | FIXED (was BREAKS-PARITY)                    | Wired the already-ported `calendarDefaultAnchor`/`calendarAnchorLabel` (existed unused in `lib/modes/surfacePrefs.ts`) into a new `anchorInfo` memo mirroring the web's next-in/next-out/payday/tightest resolution + caption logic; `jumpToTightest` replaced with `jumpToAnchor`, and the pill now shows the mode-appropriate label/caption. The views' accent-soft day highlighting still keys off the real tightest day (`lowDate`) exactly as the web does — only the pill/jump target is mode-aware. Added the "Curve →" header button (`nav.go('today')`).                                                                                                                                                                                                                                                | `apps/mobile/src/folio/screens/CalendarScreen.tsx`                                                                                                                                                                                                                            |
| ScreenCalendar                  | FIXED (was MINOR)                            | Added the "Curve →" header button next to the title block (`nav.go('today')`, matching the web's aria-label).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/mobile/src/folio/screens/CalendarScreen.tsx`                                                                                                                                                                                                                            |
| ScreenPots                      | FIXED (was BREAKS-PARITY)                    | Added the full borrow hard-cap UX: (1) `Pot.allowNegative?: boolean` field + new `setPotAllowNegative` store action + a "Can go briefly negative" toggle row per pot; (2) an `owedByPot` derivation (sum(borrow) − sum(repay) from `potLedger`, mirroring the web exactly) + a "Repay £{n}" pill + "£{n} borrowed from this pot — repay when the month allows." caption, shown whenever `owedByPot[p.id] > 0`, wired to the existing `repayToPot` store action.                                                                                                                                                                                                                                                                                                                                                  | `apps/mobile/src/folio/screens/PotsScreen.tsx`, `apps/mobile/src/folio/store.ts`                                                                                                                                                                                              |
| ScreenPots                      | FIXED (was BLOCKED)                          | Ported the web's pub/sub `reactionBus` (tiny Map<channel, Set<listener>>, no deps) to `apps/mobile/src/folio/lib/melo/reactionBus.ts` and the `MeloReaction` "margin note" component (hairline rule + Fraunces-italic whisper line, Reanimated in/out instead of CSS keyframes, reduce-motion collapses to final state) to `apps/mobile/src/folio/ui/MeloReaction.tsx`. Wired `<MeloReaction channel="pots-inline" anchor="under-row" matchKey={pot.id} />` under each pot card (mirrors the web's per-pot placement exactly), and ported the emit logic into the store's `addToPot` (goal-crossing "full" / halfway thresholds, byte-faithful mood/copy/durations).                                                                                                                                             | `apps/mobile/src/folio/lib/melo/reactionBus.ts` (new), `apps/mobile/src/folio/ui/MeloReaction.tsx` (new), `apps/mobile/src/folio/screens/PotsScreen.tsx`, `apps/mobile/src/folio/store.ts`                                                                                    |
| ScreenSubscriptions             | FIXED (was BLOCKED)                          | Same reaction-bus/`MeloReaction` port as the Pots row above. Wired `<MeloReaction channel="subs-inline" anchor="under-row" matchKey={sub.name} />` under each subscription row, and ported the emit logic into the store's `togglePaused` (pause/resume mood+copy, byte-faithful).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `apps/mobile/src/folio/screens/SubscriptionsScreen.tsx`, `apps/mobile/src/folio/store.ts`                                                                                                                                                                                     |
| ScreenInsights                  | FIXED (was BREAKS-PARITY)                    | Added the "This week, at a glance" weekly digest card (Spent / Quiet-days two-tile, 7-day trailing spend derived from the real `transactions` store, positive-toned at 3+ quiet days) directly under the tight-point chart.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `apps/mobile/src/folio/screens/InsightsScreen.tsx`                                                                                                                                                                                                                            |
| ScreenInsights                  | FIXED (was BREAKS-PARITY)                    | Ported `lib/wins.ts` (TinyWin type + `makeWin`/`hasWin`, verbatim from the web) and wired a `tinyWins?: TinyWin[]` store field + `awardTinyWin` action. Added the "Tiny wins" section (up to 4, newest first) that renders only when non-empty. **Caveat**: this ports the data shape + one-shot-per-kind guard only — the actual award call-sites (danger-date-pushed / first-10-saved / afford-streak-3+7 / bill-week-survived / first-green-after-red / first-pot-funded / first-sub-caught) are NOT wired into their real trigger points across the app (that's a much larger, separate effort spanning many screens). Until those call-sites fire, `tinyWins` stays honestly empty and the section simply doesn't render — matching the web's own `tinyWins.length > 0` guard rather than fabricating data. | `apps/mobile/src/folio/screens/InsightsScreen.tsx`, `apps/mobile/src/folio/lib/wins.ts` (new), `apps/mobile/src/folio/store.ts`                                                                                                                                               |
| ScreenInsights                  | FIXED (was BREAKS-PARITY)                    | Wired the already-ported `getRetrospect(moneyMode, cycles, potsTotal)` (existed unused in `lib/modes/retrospect.ts`) through the eyebrow, headline, both mode-tinted stat tiles (primary/secondary — the other two "In pots right now"/"Average set aside" tiles stay generic per the web), the trend caption, and a new Melo-note line under the chart. `StatTile` now takes a pre-formatted `value: string` (so mode-tinted retro values render byte-identical to the web) with an opt-in `countUpValue` for the two tiles that keep the count-up treatment.                                                                                                                                                                                                                                                   | `apps/mobile/src/folio/screens/InsightsScreen.tsx`                                                                                                                                                                                                                            |
| SheetAddEvent                   | FIXED (was MINOR)                            | Extended `SheetPayload` (`types.ts`) with `addEventKind`/`addEventTitle`, threaded a new `intent` prop through `AddEventSheet` → the shell's new `addEventIntent` slot (mirrors the `editTxnTarget`/`dayDetailDate` pattern exactly) → `nav.openSheet('add-event', {...})`. `TodayModeScreen`'s `openAddBill` now passes `{addEventKind:'out', addEventTitle:''}` matching the web's real call site.                                                                                                                                                                                                                                                                                                                                                                                                             | `apps/mobile/src/folio/sheets/AddEventSheet.tsx`, `apps/mobile/src/folio/types.ts`, `apps/mobile/src/folio/shell/FolioShell.tsx`, `apps/mobile/src/folio/screens/TodayModeScreen.tsx`                                                                                         |
| SheetAddEvent                   | NOT FIXED (documented, deliberate)           | The stepper-over-native-date-input substitution is an intentional, no-new-dependency design choice already documented in-file — left as-is; not a defect to fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/mobile/src/folio/sheets/AddEventSheet.tsx`                                                                                                                                                                                                                              |
| SheetAddPlan                    | FIXED (was BREAKS-PARITY)                    | `canAdd` now requires `byDate` to match a strict `YYYY-MM-DD` regex (not just be non-empty) — a malformed date can no longer reach `plans[]`/the Planning-lens engine's date math. The field also shows a red border + "Use YYYY-MM-DD" hint when non-empty and invalid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `apps/mobile/src/folio/sheets/AddPlanSheet.tsx`                                                                                                                                                                                                                               |
| SheetAddPlan                    | FIXED (was MINOR)                            | Added a confirmation via the existing undo/toast lib (`useUndo`/`showUndo`) — "Plan added · {name}", Undo removes the plan via a new `removePlan` call.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `apps/mobile/src/folio/sheets/AddPlanSheet.tsx`                                                                                                                                                                                                                               |
| SheetLogPayment                 | FIXED (was BREAKS-PARITY)                    | The sheet now computes `paid`/`remaining`/`cleared` itself (mirroring the store's own clamp-at-£0 logic) before calling `logDebtPayment`, and shows a confirmation via `useUndo`/`showUndo` ("{name} cleared" / "Payment logged · {name}"). Undo restores the balance via a new `undoDebtPayment` store action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `apps/mobile/src/folio/sheets/LogPaymentSheet.tsx`, `apps/mobile/src/folio/store.ts`                                                                                                                                                                                          |
| SheetEditTxn                    | FIXED (was BREAKS-PARITY)                    | Restored the editable "Merchant" TextInput as its own field row (first row, matching web order) — merchant is no longer only a read-only title fragment. Wired into the changed-check, the undo snapshot, and the `editTransaction` patch (the store engine already supported a `merchant` patch field; only the UI had dropped it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `apps/mobile/src/folio/sheets/EditTxnSheet.tsx`                                                                                                                                                                                                                               |
| SheetEditTxn                    | NOT FIXED (documented, deliberate follow-up) | Editable Date still needs a platform date-picker dependency decision — left as the pre-existing documented gap; out of scope for a no-new-dependency pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `apps/mobile/src/folio/sheets/EditTxnSheet.tsx`                                                                                                                                                                                                                               |
| SheetEditTxn                    | FIXED (was MINOR)                            | Replaced the single "Save changes" button with a two-button footer — "Cancel" (inset fill, closes without applying pending edits) + "Save changes" (accent fill), side by side, matching the web exactly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `apps/mobile/src/folio/sheets/EditTxnSheet.tsx`                                                                                                                                                                                                                               |
| SheetEditItem                   | none found                                   | Layout, copy, all field wells (Name/Amount/Date/Type chips/Note), and footer buttons match verbatim; RN's real-candidate wiring (vs the web's frozen sample) is a strict improvement, not a gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/mobile/src/folio/sheets/EditItemSheet.tsx`                                                                                                                                                                                                                              |
| SheetLogSpend / SheetLogInvoice | none found                                   | Both are faithful 1:1 ports — copy, category chips, amount card, validation gating, and CTAs all match verbatim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `apps/mobile/src/folio/sheets/LogSpendSheet.tsx`, `apps/mobile/src/folio/sheets/LogInvoiceSheet.tsx`                                                                                                                                                                          |
| ScreenTimeline                  | NOT FIXED (documented, engine-gated)         | RN's real-data upgrade is a deliberate improvement over the web's hardcoded demo; the missing verb-states depend on an unbuilt sub-pause/Review-decision event engine (`@rn-engine timeline-verbs`) — out of scope for a screen-parity pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `apps/mobile/src/folio/screens/TimelineScreen.tsx`                                                                                                                                                                                                                            |
| ScreenPlans                     | none found                                   | Faithful 1:1 port — real derived upcoming list (an intentional, documented upgrade over the web's 6 hardcoded demo rows), payday marker, CTAs, and Melo line all match.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `apps/mobile/src/folio/screens/PlansScreen.tsx`                                                                                                                                                                                                                               |
