{
  "name": "@melo/cloud-vault",
  "version": "0.0.0",
  "private": true,
  "description": "Zero-knowledge encrypted backup service for Melo. Stores opaque snapshots only.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.20.0"
  }
}
