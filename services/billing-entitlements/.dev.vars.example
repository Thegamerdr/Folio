# Local-only example. Never commit real values; use `wrangler secret put` for deployed Workers.
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
# App Store In-App Purchase key credentials (not a general Connect API key; server secrets only).
APPLE_ISSUER_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=
APPLE_ENVIRONMENT=Sandbox
ENTITLEMENT_SIGNING_PRIVATE_KEY=
