# Scenario Seed List

All seeds are synthetic and loaded through:

```text
More -> Dogfood mode
```

Seeds:

- `empty_first_launch` -> First Minute
- `sample_briefing` -> Sample briefing, no saved records
- `minimal_manual_user` -> Today
- `one_upcoming_bill` -> Plans
- `rejected_import` -> Import review
- `duplicate_rejected_import` -> Import review
- `accepted_import` -> Import review
- `edited_import` -> Import review
- `active_plan` -> Plans
- `bad_month_recovery_preview` -> Recovery preview, non-mutating
- `accepted_recovery` -> Recovery with accepted scenario/decision/audit evidence
- `document_attachment` -> Data Control
- `calendar_planner_items` -> Calendar
- `data_control_export` -> Data Control

Required owner scenarios covered:

- synthetic empty state;
- minimal manual user;
- accepted import;
- rejected import;
- active plan;
- recovery preview;
- accepted recovery.
