# Optional. Sign-in stays entirely optional and nothing in the app is feature-gated on it — with
# this unset, ClerkProvider never mounts and the AccountScreen "Sign in" row shows its honest stub
# exactly as before (see src/folio/lib/clerkAuth.ts). Set it to enable email-code sign-in.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
