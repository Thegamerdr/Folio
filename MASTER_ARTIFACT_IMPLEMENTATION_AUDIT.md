# Master Artifact — Implementation Audit (2026-06-25)

> Governing source: `FOLIO_V2_PRODUCT_UX_DECISION.md`. Scope locked to personal money-clarity.
> Verdicts are blunt. PASS = meets the artifact's acceptance criteria, evidenced by device screenshots or source. PARTIAL = partly there, named gap. FAIL = does not meet the bar.
> Honesty: this audits the build's state, not its validation. No cold-user test has run; nothing here is "validated".

| Area                    | Verdict     | Evidence / gap                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Start                   | **PASS**    | Device shot 41: one dominant "See where you stand", warm copy, secondary behind a reveal, reassurance line. No internal language.                                                                                                                                                                                                                                                                             |
| Guided input            | **PASS**    | Device shots 42/43: one question per step, "STEP n of 4" + progress from frame one, rough number allowed, answer above the fold, redundant exactness step removed.                                                                                                                                                                                                                                            |
| Review queue            | **FAIL**    | Six panels stack **above** the first row (unreadable-file, paste, latest-file, progress, "Review action" notice) — first row only visible after scrolling past instrumentation. Rows carry **no explicit state badge** (waiting/ready). Fix: rows directly under header; input/instrumentation demoted behind one "Add bank activity" reveal below the rows; per-row Waiting/Ready badge.                     |
| Row action sheet        | **PASS**    | Device shots 49/50: one visible primary "Add to my money"; Edit/Ignore/Duplicate/Transfer/Refund/Income/Bill/Debt payment/Later all behind a single "More". Do not regress.                                                                                                                                                                                                                                   |
| PDF/screenshot fallback | **PASS**    | "We can't read this file automatically yet" panel at the top with "Add the numbers yourself" + quiet keep/remove. Honest; no OCR claim. Stays at top (only shows when there are no readable rows).                                                                                                                                                                                                            |
| Today                   | **PASS**    | Device shots 36/52: one verdict, one tabular number, human source line, no contradiction, no false alarm, no jargon.                                                                                                                                                                                                                                                                                          |
| Breathing-room route    | **PASS**    | Device shot 52: twin-stroke green route, marked low point, area fill, incomplete-state guard ("Add your next income and one must-pay item…"), amber only while items await review.                                                                                                                                                                                                                            |
| Route detail reveal     | **FAIL**    | The point panel repeats a state badge ~6× (one per `RouteRow`), shows "Show why" on every line, and surfaces engine copy ("…protected after bills before optional spending", bare title "Known bills reserved"). Fix: collapse to four plain answers (what happened / what caused it / what's included / what's still waiting), state shown once, humanized engine strings.                                   |
| Debt flow               | **PARTIAL** | All §9 fields exist in `DebtGuidedScreen` (lender, balance, minimum, due date, APR, status enum, note, pressure) and a saved debt lowers route available-now — **but** status/pressure/balance/APR are flattened into one title string and never become first-class data; `pressure` is collected and unused. Fix: pass status/pressure as structured fields; show a consequence line for behind/arrangement. |
| Bill flow               | **PARTIAL** | name/amount/due/must-pay/repeats present, but `repeats` only decorates the title (not persisted) and there is **no paid/unpaid** concept. Fix: persist repeats; add paid/unpaid; a paid bill must not lower breathing room.                                                                                                                                                                                   |
| Income flow             | **FAIL**    | No dedicated flow (income captured only in guided input); **no repeats**, **no confirmed/expected** — income is stored `status:'confirmed'`, so an expected payday is a confirmed fact. (It does not inflate available-now, so the headline is safe, but the §9 rule is unenforced.) Fix: add confirmed/expected + repeats; label expected income as expected on the route.                                   |
| More                    | **PASS**    | `MoreScreen` shows only plain areas (Timeline, Calendar, Plans, What-if, Review, Data and privacy, Sample briefing) + Settings + "On this device". Nit: no explicit "What changed"/"Help" entries (coverage, not a leak).                                                                                                                                                                                     |
| Data/privacy            | **PASS**    | Plain "On this device" privacy panel; local-first framing; no cloud promises.                                                                                                                                                                                                                                                                                                                                 |
| Language gate           | **PARTIAL** | Banned terms leak into **normal** user copy: "already real" (Recovery saved ribbon badge), "manual entry"/"Manual entries"/"Manual local entry" (Money + Source surfaces), "during this replay" (First-minute body). Existing visible-copy test covers only a subset. Fix: rename to calm terms + extend the language-gate test to the full §11 list.                                                         |
| Internal/dev visibility | **PASS**    | `__DEV__`-gated `developerModeEnabled`: Dev chip, dogfood/replay rows, dev-mode toggle, counts panel all behind the gate; `developerModeEnabled` defaults false. Hardening applied: `screen === 'dogfood'` render also requires `developerModeEnabled` (belt-and-braces against a stale persisted screen).                                                                                                    |

**Summary.** 9 PASS, 3 PARTIAL (Debt, Bill, Language gate), 3 FAIL (Review queue, Route detail, Income). The PASS areas are the core path already rebuilt and device-verified this session; the FAIL/PARTIAL areas are the remaining known failures from the artifact and are the subject of this implementation. **Not ready for a serious cold-user retest until the FAIL/PARTIAL rows are closed and re-shot on a real build.**
