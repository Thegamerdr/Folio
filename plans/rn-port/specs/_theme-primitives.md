# Theme + Primitives digest

```json
{
  "primitivesDigest": "SOURCE FILES (absolute): Melo/MeloLine/EmptyState/Money/useCountUp/formatGBP = C:\\dev\\folio-melo\\.claude\\worktrees\\design-main\\src\\components\\folio\\kit.tsx · Sheet = ...\\src\\components\\folio\\sheets\\Sheet.tsx · TabBar = ...\\src\\components\\folio\\shell\\TabBar.tsx · MeloChat = ...\\src\\components\\melo\\MeloChat.tsx · tokens+keyframes = ...\\src\\styles.css · Calendar EmptyState variant = ...\\src\\components\\folio\\screens\\ScreenCalendar.tsx:359.\n\n=== IMPORTANT: NO \"Pebble\" COMPONENT EXISTS ===\nThere is no Pebble/blob component in the codebase. RN_PORT.md lists \"<Pebble> background blobs → react-native-svg static or skia\" as a FORWARD-LOOKING map entry for optional ambient background shapes — none are implemented in the web prototype (grep found zero radial-gradient/blob background usages in product screens; only the TabBar uses backdrop-blur). The MOTION.md name \"pebble-breathe\" is Melo's breathing, not a separate object. kit.tsx's header is emphatic: \"Melo is NOT a generic pebble/blob — he is a folded paper marker.\" So for RN: do NOT build a Pebble character. If you want ambient background blobs, treat as net-new (static react-native-svg ellipses tinted with --accent-soft at low opacity); otherwise omit. The only \"pebble\" you must reproduce is Melo.\n\n=== SHEET (bottom sheet) ===\nStructure: full-bleed absolutely-positioned overlay (z-30), role=\"dialog\" aria-modal, optional aria-label=title. Two layers: (1) SCRIM — a full-area pressable, color var(--ink) at 45% opacity (rgba ink @ .45 — warm ink, NOT pure black), class scrim-in (fade 320ms ease-out), tap closes. (2) SHEET BODY — anchored bottom, full width, background var(--paper) (paper, NOT surface — \"paper lifting from paper\"), top corners radius 28px, max-height 82% with overflow hidden, boxShadow var(--shadow-sheet) = 0 -8px 40px -12px rgba(26,24,21,0.18). Top edge: a 1px highlight bar, linear-gradient(90deg, transparent, rgba(255,255,255,0.7), transparent), aria-hidden — sells the lift. Grip: centered pill 36px wide x 3px tall, radius full, color var(--hairline), with pt-3 pb-1 padding. Content area: px-6 pt-2 pb-6, vertical scroll, scrollbars hidden (no-scrollbar), maxHeight calc(82vh - 24px). Entry anim class sheet-in = translateY(24px)->0 + opacity 0->1 over 480ms cubic-bezier(.16,1,.3,1) (note styles.css .sheet-in is 0.45s; MOTION.md/Sheet doc say 480ms — use 480).\nRN build: @gorhom/bottom-sheet BottomSheetModal. backdropComponent renders scrim ink@45% (animatedOpacity). handleComponent renders the 36x3 hairline grip. backgroundStyle = {backgroundColor: theme.paper, borderTopLeftRadius:28, borderTopRightRadius:28}. Add the white top-edge gradient via a 1px LinearGradient (expo-linear-gradient) absolutely at top. Shadow: iOS shadowOffset {0,-8} shadowRadius 20 shadowOpacity 0.18 ink; Android elevation. Snap point ~82% of screen height. Spring/timing on present matches the .16,1,.3,1 curve (Easing.bezier(0.16,1,0.3,1), 480ms) — see motionTable sheet-rise.\n\n=== MELO CHARACTER (the brand mascot) ===\nIdentity (non-negotiable): a chunky-outlined FOLDED-PAPER DOCUMENT — same paper as the app, \"a note that learned to feel,\" listening through two ink ear-cups. viewBox=\"0 0 40 44\", overflow visible (poses/confetti draw past edges). Wrapper is <span role=\"img\" aria-label=\"Melo, {mood}\"> OR <button> when onTap given (aria-label \"Melo · {mood}, tap to chat\", adds press class). Container width=height=size (default 28), transparent.\nSTROKE = 2.4 (single source of truth for the outline weight — structural identity, never change). fold = 7.5 units (folded corner size).\nLayer order inside the SVG (back to front):\n1. defs: radialGradient \"{uid}-lift\" (ink 0.24 -> 0 transparent) for ground shadow; linearGradient \"{uid}-grain\" vertical (white 0.85 @0% -> 0 @35%) for top paper highlight.\n2. Ground shadow (only if grounded, default true): <ellipse cx20 cy41.6 rx12.5 ry1.7 fill=url(lift)>.\n3. Confetti (celebrate only): 4 small rounded rects rotated, fills accent/caution/positive/accent.\n4. The TILT GROUP <g transform=\"rotate(tiltDeg 20 22)\" transformOrigin 20px 22px, transition transform 600ms cubic-bezier(.16,1,.3,1)>, keyed for the mood-pulse one-shot (class melo-mood-pulse), containing:\n   a. Ear cups: two ink ellipses BEHIND body — left <ellipse cx4.8 cy22 rx3.2 ry4.2 fill ink>, right <ellipse cx35.2 cy={22 - earLift*1.2} rx3.2 ry4.2 fill ink> (right ear lifts on curious; cy transitions 500ms .16,1,.3,1). They read as headphone pads.\n   b. Fold cast-shadow: small triangle under the flap, fill ink opacity 0.12.\n   c. Body: bodyPath (square w/ rounded corners + cut top-right where the fold is), fill var(--surface), stroke ink, strokeWidth 2.4, linejoin round. bodyPath = `M 9.5 5.5 L 26 5.5 L 33.5 13 L 33.5 35 Q 33.5 38.5 30 38.5 L 10 38.5 Q 6.5 38.5 6.5 35 L 6.5 9 Q 6.5 5.5 9.5 5.5 Z` (26 = 33.5-fold, 13 = 5.5+fold).\n   d. Top grain highlight: same bodyPath filled url(grain) opacity 0.35.\n   e. Folded corner: foldFillPath = `M 26 5.5 L 26 13 L 33.5 13 Z` fill var(--caution) stroke ink 2.4; plus crease line foldCreasePath = `M 26 13 L 33.5 13` stroke ink opacity 0.35 width 0.5.\n   f. Content lines (the \"writing\"): two beige (#D9CFB8) round-cap strokes width 1.6 — line1 (11.5,31)->(22.5,31), line2 (11.5,34)->(18.5,34).\n   g. Cheek blush (cheer/celebrate only): <g fill var(--accent) opacity 0.32 class melo-blush-in> two ellipses cx12.8 & 27.2, cy24.4, rx1.5 ry0.9.\n   h. Worry-bead (concern only): teardrop path near (28.5,16) fill accent opacity 0.55 stroke ink 0.45, class melo-bead-in.\n   i. Brows: concern = two angled ink strokes width1.05 opacity0.85 ( (14,17.4)->(17.4,18.4) and mirror ); curious = single stroke (22.4,17.6)->(25.6,17.0) opacity0.7.\n   j. EYES (5 expressions): open/up = two ink circles cx16 & cx24, r1.6, cy 20 (open) or 19.4 (up), each with an ASYMMETRIC white catchlight (left r0.5 at (15.5, 19.5/18.9), right r0.42 at (23.6, 19.55/18.95)) opacity 0.95; cy transitions 450ms .16,1,.3,1. closed-smile (celebrate) = two upward-arc ink paths width1.6 (\"Q ... 18 ...\" peaks up). closed-worry (concern) = two downward-arc ink paths width1.6. soft (rare) = circles r1.4 with catchlights (smaller). Blink classes melo-eye-l / melo-eye-r applied to the open/soft circles.\n   k. Mouth: <path d=spec.mouth stroke ink strokeWidth 1.7 (body-ish weight, \"carved not penned\") fill none round caps, transition d 500ms ease>.\n5. Pose badge (outside tilt group, stays upright) if pose !== none.\nSizes tier (px): 18 inline glyph · 28 default companion · 40 sheet/screen accent · 64 screen anchor · 120 hero (max twice/session). props: size, mood (calm|curious|cheer|concern|celebrate; legacy aliases soft->calm, alert->concern — DROP in RN), pose, intensity (multiplies tilt), grounded, onTap.\nRN build: react-native-svg (Svg/Path/Ellipse/Circle/Line/G/Defs/RadialGradient/LinearGradient/Stop). All \"var(--x)\" -> theme[x] from useTheme. Animate breathe + blink + tilt + mood-pulse via reanimated (see motionTable). Eyes' catchlight asymmetry and the 2.4 outline are the two things reviewers will check — keep exactly.\n\n=== MeloLine (canonical inline use) ===\nRow: <Melo size=28 mood=...> + Fraunces italic quote. Text: italic, font-display (Fraunces), 13.5px, leading-snug, color var(--muted-ink), wrapped in quotes \"{text}\", pt-1, gap 12px, items-start. RN: View row gap12 alignItems flex-start; Melo + Text{fontFamily Fraunces-Italic, fontSize13.5, lineHeight~18, color muted-ink}.\n\n=== MONEY (tabular figures) ===\n<span class=\"font-display tabular font-medium {sizeClass} {toneClass}\">{value}</span>. value is a PRE-FORMATTED string (use formatGBP). font-display = Fraunces; tabular = font-variant-numeric tabular-nums + font-feature \"tnum\",\"cv11\"; weight 500. Size map: sm 15px · md 20px · lg 28px (default) · xl 44px · hero 72px/leading-none. Tone map -> color: ink var(--ink) (default) · positive var(--positive) · negative var(--negative) · muted var(--muted-ink) · accent var(--accent).\nformatGBP(n): negative uses U+2212 MINUS \"−\" (not hyphen) then \"£\", abs value toLocaleString en-GB maximumFractionDigits 0. e.g. -1234 -> \"−£1,234\".\nRN build: <Text style={{fontFamily:'Fraunces', fontVariant:['tabular-nums'], fontWeight:'500', fontSize:SIZE[size], lineHeight: size==='hero'?72:undefined, color: theme[toneColor]}}>{value}</Text>. Money values NEVER slide — animate only via count-up (see motionTable). Replicate formatGBP with the U+2212 minus and en-GB grouping (Intl.NumberFormat works in Hermes; or manual thousands grouping).\n\n=== EmptyState (the shared primitive) ===\nCentered card: background var(--inset), radius 2xl (32px), px-6 py-8, flex col items-center text-center. Children top->bottom: <Melo size=44 mood> (default mood curious), <h3 font-display 20px leading-snug mt-4 maxW260 {headline can be ReactNode so one accent word can be colored}>, <p 13px muted-ink mt-2 leading-relaxed maxW260 {body}>, optional CTA button (press, mt-5, h-10, px-5, radius full, bg var(--ink) text var(--paper) 13px medium). One CTA max; refusal always allowed. Loading is NEVER a spinner — it's Melo curious + one line (max 4s before fallback). NOTE: ScreenCalendar.tsx has its OWN lighter EmptyState (no Melo, surface+hairline card, font-display italic 15px headline + 12px body, names the missing lever: payday/bills/pots) — that's a screen-local variant, not the shared primitive.\nRN build: View{backgroundColor inset, borderRadius32, paddingH24 paddingV32, alignItems center}; Melo44; Text headline (Fraunces 20, allow inline accent Text); Text body (sans 13, muted); Pressable CTA pill (ink bg, paper text). headline as ReactNode -> accept ReactNode children so an accent word renders <Text color=accent>.\n\n=== TabBar ===\nFixed bottom bar, height 68px, background var(--surface) @ 95% opacity + backdrop-blur-md, top border 1px var(--hairline), flex row justify-around, px-2, z-10. 4 tabs: Today (icon ◐), Review (id \"visualizer\", icon ◇), Melo (icon ●), More (icon ⋯). Each tab = button (press) flex-col items-center gap-1 py-2 px-3 with icon (14px) over label (10px tracking-wide). Active rules: today active for screens today|today-after; visualizer active for visualizer|review; melo for melo; more for a large moreSubtree list (more,timeline,calendar,plans,whatif,recovery,privacy,add-bill,add-debt,subs,pots,ritual,insights). Active state: icon -> var(--accent), label -> var(--ink) + font-medium; inactive: both var(--muted-ink). Icons are UNICODE GLYPHS (◐ ◇ ● ⋯) in the prototype — but RN_PORT.md says ship the curated lucide subset (lucide-react-native, same names) for production; treat the glyphs as placeholders, pick lucide icons that read as Today/Review/Melo/More.\nRN build: absolute bottom bar over the screen (or React Navigation custom tabBar). Use a translucent surface@95% with BlurView (expo-blur) behind, hairline top border (StyleSheet.hairlineWidth). Each tab Pressable + Haptics.selectionAsync on press (the press utility). Active tint accent, label ink+500 weight, inactive muted-ink. Respect bottom safe-area inset (the web 68px doesn't include the home indicator).\n\n=== MeloChat (the chat sheet body) ===\nColumn h-[640px] max-h-78vh. Header row: avatar slot (pass a <Melo>), \"Melo\" 14px medium + subline 11.5px muted (\"Knows your money\" if share else \"Just listening\" · {Tone label}), right \"Tune\"/\"Done\" toggle (11.5px uppercase tracking muted, press). Settings panel (toggle): Voice = 4-col grid of tone pills (Calm/Honest/Dry/Coachy), selected pill = bg ink text paper, unselected = bg inset text ink, h-8 radius-lg 12px; a \"Let Melo see my money\" checkbox row (accent checkbox) with caption \"Stays on this device\"; \"Start fresh\" link when messages exist. Empty transcript: Fraunces italic 16px \"What's on your mind?\" + 4 starter buttons (inset bg, radius-xl, px3.5 py2.5, 13px, left-aligned, press). Messages: USER bubble right-aligned, bg ink text paper, radius-2xl with rounded-br-md (tail), max-w 80%, 13.5px; ASSISTANT = plain markdown text 13.5px ink, plus tool-call pills (inset bg + hairline border, radius-xl, ✓ accent mark, uppercase 10.5px tool name, message line, optional \"Undo\" link 8s window). Loading = a Shimmer \"Melo's thinking…\" (NOT a spinner). Composer = textarea \"Say anything to Melo…\" + submit. Persists to localStorage folio.melo.chat.v1 (RN: AsyncStorage/MMKV). RN build: gorhom sheet body; FlatList transcript; bubbles as Views with the tones above; tool pills; reanimated shimmer for loading; tone pills + share toggle in a collapsible settings View.\n\nTHEME USAGE RULE across all primitives: every colour is a CSS var (--paper/--surface/--inset/--ink/--muted-ink/--hairline/--accent/--accent-soft/--positive/--caution/--negative) plus a few hard-coded warm accents that should also become theme tokens in RN: paper-content beige #D9CFB8 (Melo content lines), sealed-pose warm terracotta #E88461, seal ink #3A1208, and pure #FFFFFF catchlights/highlights. No raw colours otherwise. Fonts: display=Fraunces (embed), sans=Inter Tight/Inter (SF Pro/Roboto acceptable per RN_PORT), mono=JetBrains Mono. Radii tokens: sm8 md12 lg18 xl24 2xl32 (Tailwind class radius-2xl=32; sheet top=28 is bespoke).",
  "meloMoods": "FIVE MOODS (each = a small SVG delta on the same folded-paper body; mood is \"paper state,\" not facial mugging). Spec values from MOODS record in kit.tsx (tilt deg / eyes / mouth path / breathe class / cheek / earLift / sweat / confetti):\n\n1) CALM — DEFAULT. tilt 0; eyes \"open\" (round ink dots cy20, catchlights); mouth slight smile \"M15.5 27 Q20 29.6 24.5 27\"; breathe melo-breathe (4.4s); no cheek, no ear-lift, no bead, no confetti. Feel: just here, no demand. Visual = flat, closed, square paper. TRIGGERS: idle Today (safe verdict), empty pots/subs, quiet sheets, onboarding steps 1-3, pots empty+populated, payday ritual intro, insights populated, Melo chat sheet, privacy, Add-entry fallback, calendar \"calm\" band. Also the loading-substitute is curious not calm.\n\n2) CURIOUS — tilt -3; eyes \"up\" (pupils nudged to cy19.4) + a small right-side brow stroke; mouth gentle \"M17 27.4 Q20 26.4 23 27.4\"; breathe melo-breathe-fast (2.4s); RIGHT EAR LIFTS (earLift 1 -> right ear cy = 22-1.2). Feel: looking, gently. Visual = top-right corner peeled up / leaning in. TRIGGERS: Today tight verdict, reading a statement, spotting a sub, insight loading, ALL loading states (Melo curious + one line, never a spinner), onboarding step 4 (pots), pot-fund sheet, sub-caught sheet, insights empty, Add-entry reading, WhatIf exploring. (EmptyState default mood = curious.)\n\n3) CHEER — tilt +2; eyes \"open\"; mouth bigger smile \"M14.5 26.4 Q20 31 25.5 26.4\"; breathe melo-breathe (4.4s); CHEEK BLUSH ON (terracotta ellipses opacity 0.32, melo-blush-in). Feel: a soft yes. Visual = seal warms, slight lean, body opens a touch. TRIGGERS: pot filled, a mattering sub paused, TodayAfter (route re-drawn), onboarding complete, Add-entry success, payday ritual \"what worked\".\n\n4) CONCERN — tilt -2; eyes \"closed-worry\" (downward arcs) + two angled worry-brows; mouth slight frown \"M16.5 28.2 Q20 26.4 23.5 28.2\"; breathe melo-breathe-slow (6s — quieter, deliberately NOT fast-as-alarm); WORRY-BEAD ON at temple (accent teardrop opacity 0.55, melo-bead-in). Feel: careful, not alarmed. Visual = bottom-left corner curled inward. NEVER red, NEVER shake. TRIGGERS: shortfall coming, unrecognized sub, Today short verdict, payday ritual \"what slipped\", shortfall screen, calendar overspent/pinch bands.\n\n5) CELEBRATE — RARE (once per closed cycle, never twice per session — devalues if overused). tilt +1; eyes \"closed-smile\" (upward arcs — already closed, so NO blink); mouth widest \"M14 26 Q20 31.6 26 26\"; breathe melo-breathe (4.4s); CHEEK BLUSH ON; CONFETTI ON (4 torn-paper rects in accent/caution/positive, rotated). Feel: the moment. Visual = seal glints + tiny torn-paper confetti. TRIGGERS: cycle closed positive, payday ritual \"done\" / seal. Pairs with pose \"sealed\" max once/cycle.\n\nCROSS-CUTTING RULES: No mood = no Melo (he's not a mascot; omit if a surface doesn't earn him). Mood is decorative — copy always carries the meaning too (accessibility). Transitions are never hard swaps: tilt+fill 600ms cubic-bezier(.16,1,.3,1), mouth/eyes ~500ms ease (cy on eyes 450ms). Aliases soft->calm, alert->concern exist for legacy web screens — DROP them in RN.\n\nPOSES (orthogonal to mood — a lower-right enamel-pin badge for what's happening on the money path, drawn upright outside the tilt group, cx33 cy36 r5.4, melo-pose-in 520ms): none(default) · safe(white tick on --positive) · check(\"?\" on --caution) · thinking(4-point ink sparkle on --accent-soft, accent stroke) · reading(two ink sweep lines on --surface) · mismatch(white X on --negative) · sealed(wax-seal F glint on warm #E88461). Rules: pose is reinforcement never the only signal; don't double-signal when a verdict colour is already beside Melo; never combine mismatch+cheer or safe+concern; default none for most instances.",
  "motionTable": "Durations/easings are the source of truth from styles.css (keyframes + utility classes). MOTION.md is the spec; where styles.css and MOTION.md differ, both noted. RN = react-native-reanimated v2/v3 unless stated. Global rule: ALL motion respects reduce-motion — under AccessibilityInfo.isReduceMotionEnabled the animation COLLAPSES TO FINAL STATE INSTANTLY (not a slower version). styles.css does this via @media (prefers-reduced-motion: reduce){ animation:none; opacity:1; transform:none; stroke-dashoffset:0 }.\n\nCONTINUOUS (infinite) — Melo is the only continuously-animating thing on a quiet screen:\n- pebble-breathe / .melo-breathe — keyframes breathe: scale 1->1.04->1, opacity 1->0.94->1; 4.4s ease-in-out infinite; transform-origin 50% 60%. (MOTION.md says 4000ms; styles.css ships 4.4s — use 4400.) RN: const s=useSharedValue(1); s.value=withRepeat(withTiming(1.04,{duration:4400,easing:Easing.inOut(Easing.ease)}),-1,true); animatedProps scale s + opacity interpolate(s,[1,1.04],[1,0.94]); transformOrigin via setting the SVG group's origin (or wrap in a Group with originX/originY). \n- .melo-breathe-fast — breathe-fast scale->1.06 opacity->0.9; 2.4s ease-in-out infinite (curious/anticipation). RN: duration 2400, peak 1.06, opacity floor 0.9.\n- .melo-breathe-slow — breathe-slow scale->1.025 opacity->0.96; 6s ease-in-out infinite (concern — quieter not alarming). RN: duration 6000, peak 1.025.\n- pebble-blink / .melo-eye-l (blink) + .melo-eye-r (blink-offset) — 5.4s infinite (MOTION.md says 5000; styles.css 5.4s — use 5400). blink: scaleY 1 until 90%, scaleY 0.08 at 93%, back to 1 by 96%. blink-offset is the same but phase-shifted (88/91/94%) so L and R never blink in lockstep. NEVER on celebrate (eyes already closed arcs). RN: drive a single 5400ms loop sv; derive eye scaleY = interpolate(progress,[0,0.90,0.93,0.96,1],[1,1,0.08,1,1]); right eye uses an offset progress (e.g. start phase -0.02) so they desync. Apply scaleY to each eye circle group (transform-origin = eye center).\n- .pulse-ring — pulse-ring: scale 0.9 opacity0.7 -> scale1.8 opacity0; 1.8s ease-out infinite. Attention halo for the ONE active CTA. RN: withRepeat(withTiming over 1800, Easing.out(Easing.ease)) on a ring view, interpolate scale 0.9->1.8 & opacity 0.7->0.\n- .pointer-nudge — pointer-nudge: translateX 0->6px->0, opacity 0.6->1->0.6; 1.6s ease-in-out infinite (\"swipe to continue\"). RN: withRepeat translateX 0..6, 1600, inOut.\n- .gap-pulse — opacity 1->0.62->1; 1.6s ease-in-out infinite. (showcase/loop chrome: float-y 6s, loop-* 6s synchronized hero — web showcase only, skip in product RN.)\n\nONE-SHOT ENTRANCES:\n- route-draw / .route-draw — keyframes draw: stroke-dashoffset 1200->0; 2.2s ease-out forwards (stroke-dasharray 1200). Draws the path-to-payday line. RN: react-native-svg Path with animated strokeDashoffset (or Skia animated pathLength). const off=useSharedValue(L); off.value=withTiming(0,{duration:2200,easing:Easing.out(Easing.ease)}); set strokeDasharray=[L,L]. Mirror 2.2s.\n- count-up / useCountUp — 700ms, cubic-out easing eased = 1-(1-t)^3, JS/RAF-driven, interpolates from previous value to target. Money values ALWAYS count-up, NEVER slide. RN: useDerivedValue + withTiming(target,{duration:700,easing:Easing.out(Easing.cubic)}) feeding an Animated/Reanimated Text via useAnimatedProps or a runOnJS setter that re-formats with formatGBP each frame. (Separate cosmetic .countup class = opacity+translateY 6px over 0.8s — that's the figure's container fade, distinct from the numeric tick.)\n- sheet-rise / .sheet-in — translateY 24px->0 + opacity 0->1; 480ms cubic-bezier(.16,1,.3,1). (styles.css ships 0.45s; Sheet doc + MOTION.md say 480 — use 480.) RN: gorhom present with animationConfigs = {duration:480, easing:Easing.bezier(0.16,1,0.3,1)} (or a matching spring); pair with scrim fade.\n- scrim-in / .scrim-in — opacity 0->1 (to ink@45%); 320ms ease-out. RN: backdrop opacity withTiming(1,{duration:320,easing:Easing.out(Easing.ease)}).\n- verdict-stamp / .stamp — keyframes stamp: scale 1.6 rot -8deg opacity0 -> scale0.95 (60%) opacity1 -> scale1 rot-8deg; 600ms cubic-bezier(.34,1.56,.64,1) (back-out, overshoot). Fires exactly once per visit, never loops. RN: scale withSpring({damping:14}) from ~1.1 to 1 + opacity, keep the -8deg rotation static; or withSequence(withTiming(0.95,...),withTiming(1,...)). RN_PORT note: scale 1.1->1 + opacity, withSpring damping 14.\n- slide-in-r / .slide-in-r — translateX 28px->0 + opacity 0->1; 360ms cubic-bezier(.16,1,.3,1) (forward nav). slide-in-l mirrors (-28px). RN: withTiming translateX (RN_PORT says 24->0 240ms; styles.css 28->0 360ms — prefer styles.css 28/360 for fidelity), Easing.bezier(0.16,1,0.3,1).\n- scale-in / .scale-in — scale 0.97->1 + opacity 0->1; 320ms cubic-bezier(.16,1,.3,1) (tile/card first render). RN: scale withTiming(1,{duration:320,bezier}) from 0.97 + opacity.\n- fade-in / .fade-in (folio-fade-in) — opacity 0->1 + translateY -2px->0; 220ms cubic-bezier(.16,1,.3,1). Lightest entrance. RN: opacity+translateY withTiming 220.\n- callout-in / .callout-in — opacity 0->1 + translateY 4px->0; 600ms ease-out with 1.4s DELAY (annotation after host settles). RN: withDelay(1400, withTiming(... ,{duration:600,easing:Easing.out(Easing.ease)})).\n\nMELO-SPECIFIC ONE-SHOTS:\n- melo-mood-pulse / .melo-mood-pulse — scale 0.985->1.025(55%)->1; 520ms cubic-bezier(.16,1,.3,1); transform-origin 20px 22px. Retriggered by changing the key on the inner <g> when mood changes (web). RN: on mood change, sv=withSequence(withTiming(0.985,{duration:0}),withTiming(1.025,{duration:286}),withTiming(1,{duration:234})) or a withSpring; origin (20,22).\n- mood transition (the swap itself) — body tilt+fill transition 600ms cubic-bezier(.16,1,.3,1); eyes cy 450ms; ear cy 500ms; mouth d 500ms ease (.mood-fade does fill/stroke/d 600ms ease). RN: animate rotation (tilt deg) withTiming 600 bezier; animate eye cy / mouth-path morph withTiming 450-500. Mouth path morph: interpolate between the two cubic mouth paths (or cross-fade two Paths if morphing is hard).\n- melo-tap / .melo-tap — scale 1->0.92(35%)->1.04(70%)->1; 420ms cubic-bezier(.16,1,.3,1). Acknowledge bounce when Melo is pressable (opens chat). RN: onPress -> withSequence(0.92,1.04,1) over 420 + Haptics.selectionAsync.\n- melo-pose-in / .melo-pose-in — scale0.4 translateY2px opacity0 -> scale1.08(60%) opacity1 -> scale1; 520ms cubic-bezier(.16,1,.3,1) (soft overshoot). Pose appears once, then static. RN: withTiming/withSpring scale to 1.08 then settle 1, origin = badge center (33,36).\n- melo-blush-in / .melo-blush-in — opacity 0->0.32 + scale 0.6->1; 380ms cubic-bezier(.16,1,.3,1) (cheek blush on cheer/celebrate).\n- melo-bead-in / .melo-bead-in — opacity 0->0.55 + translateY -3px->0(60% to 0.6px overshoot); 520ms cubic-bezier(.16,1,.3,1) (worry-bead on concern).\n\nPRESS — .press — transform transition 120ms ease; :active scale 0.97. ALL tappables. RN: Pressable -> onPressIn scale 0.97 (withTiming 120) / onPressOut back to 1, + Haptics.selectionAsync (expo-haptics) to stand in for the web hover/press feel.\n\nEASING CHEAT SHEET: cubic-bezier(.16,1,.3,1) = Easing.bezier(0.16,1,0.3,1) (the house \"settle\" curve, used everywhere); back-out cubic-bezier(.34,1.56,.64,1) = Easing.bezier(0.34,1.56,0.64,1) (verdict-stamp overshoot); cubic-out for count-up = Easing.out(Easing.cubic) (=1-(1-t)^3); ease-out = Easing.out(Easing.ease). RULES: one motion per element; money never slides (count-up only); verdict-stamp once per visit; sheets always sheet-rise+scrim-in (never fade/top); reduced motion = final state, off, not \"calm but on\".",
  "rnThemeNotes": "TOKEN SOURCE: styles.css :root (light) and :root.dark (web-showcase inverse). The RN product app keeps the warm-paper world; dark is \"web showcase only\" per the file comment, but it's a clean inverse so ship it as a second theme so both moods are previewable. Tailwind @theme inline also maps semantic aliases (background=paper, foreground=ink, card=surface, primary=accent, primary-foreground=#FFFFFF, secondary/muted=inset, muted-foreground=muted-ink, border/input=hairline, ring=accent) — replicate those aliases so component code can read either the raw or semantic name.\n\nLIGHT (:root):\npaper #F7F6F1 · surface #FFFFFF · inset #FCFBF7 · ink #1A1815 · mutedInk #6B6760 · hairline #ECE9E0 · accent #E0633A · accentSoft #F5E4DB · positive #3E8E5A · caution #D9A441 · negative #C5503E · deviceChrome #0a0907. colorScheme light.\nDARK (:root.dark):\npaper #15130F · surface #1E1B17 · inset #1A1814 · ink #F4F0E6 · mutedInk #9A938A · hairline #2A2620 · accent #EC7A52 (warmed so it sings on dark) · accentSoft #3A241C · positive #6FB388 · caution #E6BB6A · negative #E07560 · deviceChrome #050403. colorScheme dark.\nNON-TOKEN CONSTANTS to promote into the theme (currently hard-coded in kit.tsx — needed for Melo): contentLine #D9CFB8 (beige writing lines), sealWarm #E88461 (sealed pose fill), sealInk #3A1208 (seal F mark), and onAccent/catchlight #FFFFFF. Add these to BOTH themes (in dark, reconsider contentLine to something readable on surface, e.g. a muted warm tone; #D9CFB8 still works but tune if it looks too bright).\n\nRADII (from @theme): radius.sm 8 · md 12 · lg 18 · xl 24 · 2xl 32. Bespoke: sheet top corner 28, grip 999/full, CTA pill 999/full. TYPOGRAPHY: display 'Fraunces' (also italic for MeloLine/quotes; letterSpacing -0.02em on display headings, optical sizing auto) · sans 'Inter Tight'/'Inter' (RN_PORT allows SF Pro/Roboto for body) · mono 'JetBrains Mono'. tabular = fontVariant ['tabular-nums'] (web also enables feature 'tnum','cv11'; base body uses 'ss01','cv11'). SHADOWS: card 0 1px0 + 0 12px28 -16 ink@.12; float 0 2px0 + 0 24px48 -20 ink@.18; sheet 0 -8px40 -12 ink@.18 (RN: model with shadowColor ink, shadowOffset/Radius/Opacity per token; Android elevation approximations). hairline border = StyleSheet.hairlineWidth, color hairline.\n\nTHEME OBJECT shape (TS):\ntype ThemeColors = { paper, surface, inset, ink, mutedInk, hairline, accent, accentSoft, positive, caution, negative, deviceChrome, contentLine, sealWarm, sealInk, onAccent } each string;\ntype Theme = { mode:'light'|'dark'; colors:ThemeColors; radius:{sm,md,lg,xl,xxl:number; sheet:28}; fonts:{display, displayItalic, sans, mono:string}; fontSize: money map {sm15,md20,lg28,xl44,hero72} + body scales; shadow:{card,float,sheet: RN shadow style}; space, motion: { settle: Easing.bezier(0.16,1,0.3,1), backOut: Easing.bezier(0.34,1.56,0.64,1), durations:{fade220, scale320, slide360, sheet480, stamp600, breathe4400, breatheFast2400, breatheSlow6000, blink5400, route2200, count700} } }.\nconst lightTheme: Theme = {...}; const darkTheme: Theme = {...} (same shape, swapped colors + mode).\n\nuseTheme HOOK + provider:\n- ThemeContext = createContext<Theme>(lightTheme). ThemeProvider resolves mode from app preference (default light — RN_PORT: \"Don't default to dark mode\"; product = warm paper) OR Appearance.getColorScheme() if you want OS-driven; subscribe via Appearance.addChangeListener and update. export const useTheme = () => useContext(ThemeContext).\n- For dynamic styles use a makeStyles(theme => StyleSheet.create({...})) pattern or inline style arrays — the memory note records the existing RN app already uses a kitTheme/useTheme/makeStyles pattern with dark mode, so MATCH that existing convention rather than inventing a new one. Components read theme.colors.X wherever the web used var(--X); replace every \"var(--x)\" string 1:1.\n- SVG (Melo, poses, route) reads theme colors as fill/stroke props (react-native-svg accepts color strings) — pass theme.colors.ink / .surface / .caution / .accent etc.; the 4 hard-coded warm constants (contentLine, sealWarm, sealInk, white) come from theme too.\n- Paper grain (web .paper-grain feTurbulence overlay) has no cheap RN equivalent; either ship a pre-baked tiled noise PNG at low opacity (multiply-ish via overlay) or omit — it's atmosphere, not load-bearing. Reduced-data/reduced-motion should drop it.\n- IMPORTANT do-not: RN_PORT forbids introducing a SECOND colour/font/spacing scale — this one theme object is the only system. Don't add palette colours beyond the tokens above; one accent per screen."
}
```
